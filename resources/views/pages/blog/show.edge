@include('partials/header')

<article class="blog-article">
    <h1>{{title}}</h1>
    <div class="blog-article_date">{{ createdAt.setLocale('fr').toFormat('dd MMMM yyyy') }}</div>
    <div>{{{formattedContent}}}</div>
</article>

@include('partials/footer')